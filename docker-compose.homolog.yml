services:
  imobiliaria-frontend:
    build:
      context: .
      args:
        NEXT_PUBLIC_WA_NUMBER: 5517996383708
        NEXT_PUBLIC_API_URL: https://apianaerichard.visualizeaquiseu.app.br
    container_name: imobiliaria-frontend-homolog
    ports:
      - "9091:3000"
    environment:
      - API_ORIGIN=http://host.docker.internal:9090
    restart: unless-stopped
